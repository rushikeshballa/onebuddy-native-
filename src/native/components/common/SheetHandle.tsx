import React from 'react';
import { View } from 'react-native';
import { styles } from '../../screens/Security/styles';

export const SheetHandle = () => <View style={styles.sheetHandle} />;

export default SheetHandle;
