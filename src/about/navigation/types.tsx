// navigation/types.ts
// Central type definitions for the navigation stack.
// Keeping this in one file means every screen gets full
// type-checking + autocomplete on navigation.navigate(...) calls.

export type RootStackParamList = {
  About: undefined;
  AppVersion: undefined;
  TermsOfService: undefined;
  PrivacyPolicy: undefined;
};
